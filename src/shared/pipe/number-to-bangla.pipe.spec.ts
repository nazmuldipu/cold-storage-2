import { NumberToBanglaPipe } from './number-to-bangla.pipe';

describe('NumberToBanglaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToBanglaPipe();
    expect(pipe).toBeTruthy();
  });
});
