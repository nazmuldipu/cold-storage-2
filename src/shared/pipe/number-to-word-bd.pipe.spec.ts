import { NumberToWordBdPipe } from './number-to-word-bd.pipe';

describe('NumberToWordBdPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToWordBdPipe();
    expect(pipe).toBeTruthy();
  });
});
